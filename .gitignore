# Build output. The dev build lives at the repo root so that scripts/ and
# snippets/ sit beside it, which is exactly the layout a release ships in.
bin/
*.exe

# Go
*.test
*.out

# The generated resource object (icon + DPI manifest) is checked in on purpose
# once it exists -- it is a build input, not an artifact. Do not ignore *.syso.

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
