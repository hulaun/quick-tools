{
  "_comment": "Copy this file to env.json, which is gitignored because it holds hostnames and credentials. Each top-level key is an environment; Ctrl+E cycles them in the order written here, and the active one is shown beside the tabs. Values may be strings, numbers or booleans -- a number keeps the exact digits you wrote, so an id larger than a JavaScript float can hold is safe.",

  "local": {
    "base": "http://localhost:8080",
    "user": "admin",
    "tenant": 9007199254740993
  },

  "dev": {
    "base": "https://api.dev.internal",
    "user": "svc-dev"
  },

  "prod": {
    "base": "https://api.corp",
    "user": "svc"
  }
}
